class UTIL_SortContact_TEST {
  {
        list<AggregateResult> queryAggResult 
= [SELECT count_distinct(Id), General_Accounting_Unit__r.Name FROM Allocation__c WHERE Opportunity__r.Name LIKE 'opp%' GROUP BY General_Accounting_Unit__r.Name ORDER BY count_distinct(Id)];
}
}
