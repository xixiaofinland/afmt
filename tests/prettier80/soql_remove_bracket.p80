public class A {
  {
    String s = [
      SELECT AccountId
      FROM Contact
      WHERE
        AccountId IN :accountIdsToUpdate
        AND Deceased__c != TRUE
        AND Exclude_from_Household_Name__c != TRUE
    ];
  }
}
