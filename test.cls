public class Me {
  {
    String a = 'A';
    List<Account> accs = [
        SELECT Id, Name, Good__c, Bad__c, hello__c, WhatTTTTTTTTTTTT__c, goodstuff__c FROM Account WHERE Name = 'hellooooooooooo'
      ];
  }
}

